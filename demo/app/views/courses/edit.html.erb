<h1>Editing course</h1>

<%= render 'form' %>

<% if allowed?(:courses, :show, @domain.name) %>
<%= link_to 'Show', course_path(@domain.name, @course) %> |
<% end %>
<% if allowed?(:courses, :index, @domain.name) %>
<%= link_to 'Back', courses_path(@domain.name) %>
<% end %>
